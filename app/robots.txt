import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/private/', // أي مجلد لا تريد أرشفته
    },
    sitemap: 'https://tasty-recipes.vercel.app/sitemap.xml', // رابط السايت ماب الخاص بك
  };
}